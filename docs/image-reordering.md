# 画像順の調整

## 現行フロー

1. API候補はDOM順で保存画面へ渡される。
2. 選択状態は候補オブジェクトの`isSelected`で保持される。
3. 選択候補は配列順にBlob取得され、`displayOrder`が0から採番される。
4. Repositoryと閲覧画面は`displayOrder`昇順で画像を返す。
5. 話・作品のサムネイルは最小の`displayOrder`を持つ画像を使用する。

## 仕様

- 候補一覧全体のDOM由来の表示順は変更しない。
- 選択画像だけに独立した保存順を持たせ、未選択画像は保存順に含めない。
- 再選択した画像は選択画像の末尾へ追加する。
- 上、下、先頭、末尾への移動で候補オブジェクト自体の状態を維持する。
- 調整済みの選択順でBlobを取得し、`displayOrder`を0から欠番なく採番する。
- URL・画像重複警告や保存失敗から戻っても調整済み順序を保持する。

## 品質確認

- Frontend：`format`、`lint`、`type-check`、`test:unit`が成功（50 files / 304 tests passed）
- Frontend：本番ビルドとPWA生成が成功
- 並び替え、選択・未選択混在、全解除後の再選択、保存時だけの順序変換を単体テストで確認
- `createRegistrationImages`と登録処理が配列順に`displayOrder`を0から再採番することを既存データベーステストで確認
- Repositoryと閲覧プレゼンターが`displayOrder`昇順で画像を返し、最小順の画像をサムネイルに使うことを確認
- 3登録モードが共通の保存処理を通るため、調整済み順序が新規作品、既存作品への話追加、単独の話へ同じように適用される

## 実ブラウザー確認

- 3画像を個別選択し、保存順1〜3が表示されることを確認
- 上・下ボタンで2番目と3番目を入れ替え、表示が即時更新されることを確認
- 操作ボタンに候補番号と移動方向のアクセシブル名があることを確認
- 先頭の上移動と末尾の下移動が無効で、`title`に理由が表示されることを確認
- 各操作領域が44×44pxで、3列カードを維持することを確認
- 情報入力へ進んで画像確認へ戻っても、調整済み順序が保持されることを確認

実サイトの一部プレビューでは、既知の画像プロキシ上流エラーが発生した。順序操作はプレビュー成否と独立しており、候補状態と順序が保持されることを確認した。
